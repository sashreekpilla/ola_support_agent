# Evaluation folder

`cases.json` contains the required 15 test queries: all 12 policy topics, one ticket-status query, and two deliberately out-of-scope questions.

`results.json` is generated by:

```powershell
python -m app.pipeline evaluate_rag
```

The report contains per-query Accuracy, Grounding, Completeness and Safety plus the four averages.
